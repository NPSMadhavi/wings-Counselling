# Copy to apps/admin/.env and fill in values.
# This file is safe to commit — it contains no secrets.

# Backend API URL used by the admin frontend.
# In development this can be empty (Vite proxies /api to localhost:5001).
# In production set this to your backend domain, e.g. https://api.wings.org
VITE_API_URL=
